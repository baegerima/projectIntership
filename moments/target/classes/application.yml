spring:
  datasource:
    url: jdbc:postgresql://autorack.proxy.rlwy.net:27483/railway
    username: postgres
    password: RubnRPVYuFrvGHEktmUUjRGfAIarBrRf

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate.dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true

  server:
    port: 8080

  main:
    allow-circular-references: true

logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql: TRACE
